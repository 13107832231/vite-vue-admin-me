import {
  create, NButton, NConfigProvider, NInput, NDatePicker, NSpace, NMenu, NLayout,
  NLayoutSider, NAvatar, NDropdown
} from 'naive-ui'

const naive = create({
  components: [NButton, NConfigProvider, NInput, NDatePicker, NSpace, NMenu, NLayout, NLayoutSider, NAvatar, NDropdown]
})
export default naive
